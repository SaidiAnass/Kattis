#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main() {


    int Table[3][2];
    int output0, output1;
    int d1, d2;

    for(int i=0; i<3; i++) {
        scanf("%d%d", &d1, &d2);
        Table[i][0] = d1;
        Table[i][1] = d2;
    }


    if(Table[0][0] == Table[1][0]) {
        output0 = Table[2][0];
    }
    else if(Table[1][0] == Table[2][0]) {
        output0 = Table[0][0];
    }

    else if (Table[0][0] == Table[2][0]) {
        output0 = Table[1][0];
    }

    if(Table[0][1] == Table[1][1]) {
        output1 = Table[2][1];
    }
    else if(Table[0][1] == Table[2][1]) {
        output1 = Table[1][1];
    }

    else if (Table[1][1] == Table[2][1]) {
        output1 = Table[0][1];
    }

    printf("%d %d", output0, output1);

    return 0;
}
